## 1. 修改主题初始化脚本

- [x] 1.1 在 `src/layouts/Layout.astro` 的 `<head>` 内联脚本中添加 `astro:after-swap` 事件监听器
- [x] 1.2 在事件处理器中读取 `localStorage` 中的主题偏好并应用到 `<html>` 元素

## 2. 测试验证

- [x] 2.1 手动测试：在页面 A 切换到暗黑模式，导航到页面 B，验证暗黑模式保持
- [x] 2.2 手动测试：在页面 A 切换到日间模式，导航到页面 B，验证日间模式保持
- [x] 2.3 手动测试：刷新页面，验证主题状态正确恢复（无闪烁）
