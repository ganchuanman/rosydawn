原生集成 plantuml 和 drawio 

将所有的脚手架流程接入 AI，用户可以通过脚手架语义化对话，完成文章的创建，提交，部署等行为。收拢脚手架的逻辑，不再是通过 npm run xxx 的方式运行，而是 rosydawn xxx

返回路由优化，把所有的返回按钮抽成一个 astro 组件，然后在这个组件里，做两件事情，根据当前的页面来源，知道返回按钮的文案是什么以及点击返回应该路由到哪个页面，比如文章详情页如果来源是文章列表页，那么返回按钮就应该展示为返回文章列表，点击返回回到文章列表，再比如文章详情页如果来源是标签详情页，那么返回就应该展示为返回标签详情页点击返回回到标签详情页

